import { CurrentPlayerModel } from './current-player-model';

describe('CurrentPlayerModel', () => {
  it('should create an instance', () => {
    expect(new CurrentPlayerModel('some test id')).toBeTruthy();
  });
});
